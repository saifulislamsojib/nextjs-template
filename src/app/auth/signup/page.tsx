import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Signup | App',
};

const SignupPage = () => {
  return (
    <div>
      <h1>Signup Page</h1>
    </div>
  );
};

export default SignupPage;
