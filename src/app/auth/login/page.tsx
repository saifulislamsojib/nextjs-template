import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login | App",
};

const LoginPage = () => {
  return (
    <div>
      <h1>Login Page</h1>
    </div>
  );
};

export default LoginPage;
